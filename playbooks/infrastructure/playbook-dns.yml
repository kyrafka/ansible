---
- name: Configurar DNS/BIND9
  hosts: localhost
  connection: local
  become: true
  
  vars_files:
    - ../../group_vars/all.yml
    - ../../group_vars/all.vault.yml
  
  roles:
    - dns_bind
  
  tags: dns