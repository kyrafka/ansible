---
# ════════════════════════════════════════════════════════════════
# 🪟 PLAYBOOK PARA CONFIGURAR WINDOWS 11
# ════════════════════════════════════════════════════════════════

- name: Configurar Windows 11
  hosts: windows
  gather_facts: no
  
  roles:
    - windows11
  
  tasks:
    # ════════════════════════════════════════════════════════════════
    # PASO 1: CREAR USUARIOS
    # ════════════════════════════════════════════════════════════════
    
    - name: Crear usuario 'dev'
      ansible.windows.win_shell: |
        net user dev 123!123 /add
        exit 0
      ignore_errors: yes
      register: user_dev
    
    - name: Crear usuario 'cliente'
      ansible.windows.win_shell: |
        net user cliente 123!123 /add
        exit 0
      ignore_errors: yes
      register: user_cliente
    
    # ════════════════════════════════════════════════════════════════
    # PASO 2: CREAR CARPETAS
    # ════════════════════════════════════════════════════════════════
    
    - name: Crear carpetas del sistema
      ansible.windows.win_shell: |
        New-Item -Path C:\Compartido -ItemType Directory -Force | Out-Null
        New-Item -Path C:\Dev -ItemType Directory -Force | Out-Null
        exit 0
      ignore_errors: yes
    
    # ════════════════════════════════════════════════════════════════
    # PASO 3: CONFIGURAR FIREWALL
    # ════════════════════════════════════════════════════════════════
    
    - name: Configurar firewall (Ping + Compartir archivos)
      ansible.windows.win_shell: |
        New-NetFirewallRule -Name "ICMPv6-In" -DisplayName "ICMPv6 Ping" -Protocol ICMPv6 -IcmpType 8 -Enabled True -Direction Inbound -Action Allow -ErrorAction SilentlyContinue | Out-Null
        Enable-NetFirewallRule -DisplayGroup "File and Printer Sharing" -ErrorAction SilentlyContinue | Out-Null
        exit 0
      ignore_errors: yes
    
    # ════════════════════════════════════════════════════════════════
    # PASO 4: VERIFICAR CONFIGURACIÓN
    # ════════════════════════════════════════════════════════════════
    
    - name: Obtener lista de usuarios
      ansible.windows.win_shell: |
        Get-LocalUser | Select-Object Name, Enabled | Format-Table -AutoSize
      register: users_list
    
    - name: Obtener carpetas creadas
      ansible.windows.win_shell: |
        Get-ChildItem C:\ | Where-Object {$_.Name -match 'Compartido|Dev'} | Select-Object Name | Format-Table -AutoSize
      register: folders_list
    
    # ════════════════════════════════════════════════════════════════
    # RESUMEN FINAL
    # ════════════════════════════════════════════════════════════════
    
    - name: Mostrar resumen de configuración
      debug:
        msg:
          - "════════════════════════════════════════"
          - "✅ CONFIGURACIÓN COMPLETADA"
          - "════════════════════════════════════════"
          - ""
          - "👥 Usuarios creados:"
          - "  - dev (contraseña: 123!123)"
          - "  - cliente (contraseña: 123!123)"
          - ""
          - "📁 Carpetas creadas:"
          - "  - C:\\Compartido"
          - "  - C:\\Dev"
          - ""
          - "🔥 Firewall configurado:"
          - "  - Ping (ICMPv6) permitido"
          - "  - Compartir archivos habilitado"
          - ""
          - "════════════════════════════════════════"
    
    - name: Mostrar usuarios de Windows
      debug:
        var: users_list.stdout_lines
    
    - name: Mostrar carpetas creadas
      debug:
        var: folders_list.stdout_lines
