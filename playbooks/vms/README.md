#  Playbooks de VMs

Playbooks para crear y configurar VMs.

## Playbooks disponibles:

- **create-vm-ubuntu-desktop.yml**: Crea VM Ubuntu Desktop en ESXi
- **configure-ubuntu-desktop.yml**: Configura usuarios en la VM
- **create-vm-gamecenter.yml**: Crea VM con configuración específica
