---
# Inventario de hosts para Ansible

all:
  children:
    servers:
      hosts:
        localhost:
          ansible_connection: local
    
    ubuntu_desktops:
      hosts:
        ubuntu123:
          ansible_host: 2025:db8:10::dce9
          ansible_user: administrador
          ansible_ssh_pass: "123"
          ansible_become_pass: "123"
      
      vars:
        ansible_python_interpreter: /usr/bin/python3
