---
# Configuración avanzada del firewall con fail2ban y monitoreo

- name: "Instalar firewall y herramientas de seguridad"
  apt:
    name:
      - ufw
      - fail2ban
      - iptables-persistent
      - netfilter-persistent
    state: present
    update_cache: yes
  tags: firewall

- name: "✓ Paquetes instalados"
  debug:
    msg: "→ UFW, fail2ban e iptables-persistent instalados"
  tags: firewall

- name: "🔍 Detectando ubicación de ufw"
  command: which ufw
  register: ufw_path
  changed_when: false
  tags: firewall

- name: "📍 UFW encontrado"
  debug:
    msg: "→ Ubicación: {{ ufw_path.stdout }}"
  tags: firewall

- name: "🔄 Deshabilitando UFW temporalmente"
  command: "{{ ufw_path.stdout }} --force disable"
  tags: firewall

- name: "✓ UFW deshabilitado"
  debug:
    msg: "→ Preparando para configuración limpia"
  tags: firewall

- name: "🔄 Reseteando configuración UFW"
  command: "{{ ufw_path.stdout }} --force reset"
  tags: firewall

- name: "✓ UFW reseteado"
  debug:
    msg: "→ Configuración anterior eliminada"
  tags: firewall

- name: "🛡️  Configurando política: denegar entrada"
  command: "{{ ufw_path.stdout }} default deny incoming"
  tags: firewall

- name: "🛡️  Configurando política: permitir salida"
  command: "{{ ufw_path.stdout }} default allow outgoing"
  tags: firewall

- name: "✓ Políticas configuradas"
  debug:
    msg:
      - "→ Entrada: DENY (seguro por defecto)"
      - "→ Salida: ALLOW (sin restricciones)"
  tags: firewall

- name: "🔐 Permitiendo SSH con rate limiting"
  command: "{{ ufw_path.stdout }} limit 22/tcp comment 'SSH con rate limiting'"
  tags: firewall

- name: "✓ SSH protegido"
  debug:
    msg: "→ Puerto 22/tcp con limitación de conexiones"
  tags: firewall

- name: "🌐 Permitiendo DNS TCP (puerto 53)"
  command: "{{ ufw_path.stdout }} allow 53/tcp comment 'DNS TCP'"
  tags: firewall

- name: "🌐 Permitiendo DNS UDP (puerto 53)"
  command: "{{ ufw_path.stdout }} allow 53/udp comment 'DNS UDP'"
  tags: firewall

- name: "✓ DNS habilitado"
  debug:
    msg: "→ Puerto 53 TCP/UDP abierto"
  tags: firewall

- name: "📡 Permitiendo DHCPv6 Server (puerto 547)"
  command: "{{ ufw_path.stdout }} allow 547/udp comment 'DHCPv6 Server'"
  tags: firewall

- name: "📡 Permitiendo DHCPv6 Client (puerto 546)"
  command: "{{ ufw_path.stdout }} allow 546/udp comment 'DHCPv6 Client'"
  tags: firewall

- name: "✓ DHCP habilitado"
  debug:
    msg: "→ Puertos 546-547 UDP abiertos"
  tags: firewall

- name: "📁 Permitiendo puertos pasivos FTP"
  command: "{{ ufw_path.stdout }} allow 21000:21010/tcp comment 'FTP Passive Ports'"
  tags: firewall

- name: "✓ FTP configurado"
  debug:
    msg: "→ Puertos 21000-21010 TCP abiertos"
  tags: firewall

- name: "Aplicar reglas por rol de VM"
  include_tasks: filter-by-role.yml
  when: groups['ubuntu_desktops'] is defined
  tags: firewall

- name: "🔥 Habilitando UFW"
  command: "{{ ufw_path.stdout }} --force enable"
  changed_when: true
  tags: firewall

- name: "✓ UFW habilitado"
  debug:
    msg: "→ Firewall activo y funcionando"
  tags: firewall

- name: "Copiar filtros personalizados de fail2ban"
  include_tasks: copy-filters.yml
  tags: [firewall, fail2ban]

- name: "Configurar fail2ban"
  template:
    src: jail.local.j2
    dest: /etc/fail2ban/jail.local
    backup: yes
  notify: restart fail2ban
  tags: [firewall, fail2ban]

- name: "Habilitar y iniciar fail2ban"
  systemd:
    name: fail2ban
    enabled: yes
    state: started
  tags: [firewall, fail2ban]

- name: "Crear script de monitoreo de firewall"
  template:
    src: firewall-monitor.sh.j2
    dest: /usr/local/bin/firewall-monitor.sh
    mode: '0755'
    owner: root
    group: root
  tags: firewall

- name: "Crear enlace simbólico para fácil acceso"
  file:
    src: /usr/local/bin/firewall-monitor.sh
    dest: /usr/local/bin/fw-monitor
    state: link
  tags: firewall

- name: "Incluir seguridad avanzada (opcional)"
  include_role:
    name: seguridad
  when: enable_advanced_security | default(false)
  tags: [firewall, seguridad]

- name: "Mostrar estado del firewall"
  command: ufw status verbose
  register: firewall_status
  changed_when: false
  tags: firewall

- name: "Mostrar configuración aplicada"
  debug:
    var: firewall_status.stdout_lines
  tags: firewall
