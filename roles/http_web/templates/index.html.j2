<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ ansible_hostname }} - Servidor Web</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
        }
        .container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 40px;
            max-width: 600px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        }
        h1 { font-size: 2.5em; margin-bottom: 20px; }
        .info { background: rgba(255, 255, 255, 0.2); padding: 20px; border-radius: 10px; margin: 20px 0; }
        .info-item { margin: 10px 0; display: flex; justify-content: space-between; }
        .label { font-weight: bold; }
        .status { color: #4ade80; font-size: 1.2em; }
        .footer { text-align: center; margin-top: 20px; opacity: 0.8; }
    </style>
</head>
<body>
    <div class="container">
        <h1>🚀 Servidor Web Activo</h1>
        <div class="status">✓ Nginx funcionando correctamente</div>
        
        <div class="info">
            <div class="info-item">
                <span class="label">Hostname:</span>
                <span>{{ ansible_hostname }}</span>
            </div>
            <div class="info-item">
                <span class="label">Sistema:</span>
                <span>{{ ansible_distribution }} {{ ansible_distribution_version }}</span>
            </div>
            <div class="info-item">
                <span class="label">Servidor Web:</span>
                <span>Nginx</span>
            </div>
            <div class="info-item">
                <span class="label">IPv6:</span>
                <span>{{ ansible_default_ipv6.address | default('Configurando...') }}</span>
            </div>
        </div>

        <div class="footer">
            <p>Proyecto de Sistemas Operativos</p>
            <p>Configurado con Ansible</p>
        </div>
    </div>
</body>
</html>
