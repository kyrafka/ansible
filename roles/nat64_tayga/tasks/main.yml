---
# Configuración de NAT64 con Tayga para traducción IPv6 → IPv4

- name: "📦 Instalar Tayga (NAT64)"
  ansible.builtin.apt:
    name: tayga
    state: present
    update_cache: yes
  become: true

- name: "✓ Tayga instalado"
  ansible.builtin.debug:
    msg: "→ NAT64 disponible para traducción IPv6 → IPv4"

- name: "🔧 Habilitar IP forwarding IPv4"
  ansible.posix.sysctl:
    name: net.ipv4.ip_forward
    value: '1'
    sysctl_set: yes
    state: present
    reload: yes
  become: true

- name: "🔧 Habilitar IP forwarding IPv6"
  ansible.posix.sysctl:
    name: net.ipv6.conf.all.forwarding
    value: '1'
    sysctl_set: yes
    state: present
    reload: yes
  become: true

- name: "✓ IP forwarding habilitado"
  ansible.builtin.debug:
    msg: "→ Forwarding IPv4 e IPv6 activo"

- name: "🧹 Detener Tayga si está corriendo"
  ansible.builtin.systemd:
    name: tayga
    state: stopped
  become: true
  failed_when: false

- name: "🧹 Limpiar interfaz nat64 existente"
  ansible.builtin.shell: |
    # Detener cualquier proceso tayga corriendo
    pkill -9 tayga 2>/dev/null || true
    sleep 1
    # Eliminar interfaz nat64 si existe
    if ip link show nat64 &>/dev/null; then
      ip link set nat64 down 2>/dev/null || true
      ip link delete nat64 2>/dev/null || true
    fi
    # Limpiar directorio de datos
    rm -rf /var/db/tayga/*
  become: true
  changed_when: false

- name: "📝 Configurar Tayga"
  ansible.builtin.template:
    src: tayga.conf.j2
    dest: /etc/tayga.conf
    owner: root
    group: root
    mode: '0644'
  become: true

- name: "📁 Crear directorio de datos de Tayga"
  ansible.builtin.file:
    path: /var/db/tayga
    state: directory
    owner: root
    group: root
    mode: '0755'
  become: true

- name: "� ICrear script de post-inicio para rutas NAT64"
  ansible.builtin.copy:
    content: |
      #!/bin/bash
      # Script para configurar rutas después de que Tayga inicie
      
      sleep 2
      ip link set nat64 up 2>/dev/null || true
      ip -6 route add 64:ff9b::/96 dev nat64 2>/dev/null || true
      ip -4 route add 192.168.255.0/24 dev nat64 2>/dev/null || true
    dest: /usr/local/bin/setup-nat64-routes.sh
    owner: root
    group: root
    mode: '0755'
  become: true

- name: "📝 Configurar servicio systemd de Tayga"
  ansible.builtin.copy:
    content: |
      [Unit]
      Description=Tayga NAT64 daemon
      After=network.target
      
      [Service]
      Type=simple
      ExecStartPre=-/usr/sbin/tayga --mktun
      ExecStart=/usr/sbin/tayga --nodetach
      ExecStartPost=/bin/sleep 2
      ExecStartPost=-/usr/local/bin/setup-nat64-routes.sh
      ExecStopPost=-/usr/sbin/tayga --rmtun
      TimeoutStartSec=60s
      Restart=on-failure
      RestartSec=5s
      
      [Install]
      WantedBy=multi-user.target
    dest: /etc/systemd/system/tayga.service
    owner: root
    group: root
    mode: '0644'
  become: true

- name: "🔄 Recargar systemd"
  ansible.builtin.systemd:
    daemon_reload: yes
  become: true

- name: "🔄 Habilitar e iniciar Tayga"
  ansible.builtin.systemd:
    name: tayga
    state: restarted
    enabled: yes
  become: true
  register: tayga_start

- name: "⏳ Esperar a que interfaz nat64 exista (máximo 30 segundos)"
  ansible.builtin.shell: |
    for i in {1..30}; do
      if ip link show nat64 &>/dev/null; then
        echo "Interfaz nat64 encontrada después de $i segundos"
        exit 0
      fi
      sleep 1
    done
    echo "ERROR: Interfaz nat64 no se creó después de 30 segundos"
    exit 1
  become: true
  register: nat64_wait
  changed_when: false

- name: "✓ Interfaz nat64 creada"
  ansible.builtin.debug:
    msg: "→ {{ nat64_wait.stdout }}"

- name: "🛡️ Configurar NAT para Tayga"
  ansible.builtin.shell: |
    # NAT para el tráfico que sale de Tayga hacia internet
    iptables -t nat -C POSTROUTING -s 192.168.255.0/24 -o {{ wan_interface }} -j MASQUERADE 2>/dev/null || \
    iptables -t nat -A POSTROUTING -s 192.168.255.0/24 -o {{ wan_interface }} -j MASQUERADE
  become: true
  changed_when: false

- name: "✓ NAT configurado para Tayga"
  ansible.builtin.debug:
    msg: "→ Pool 192.168.255.0/24 → MASQUERADE → {{ wan_interface }}"

- name: "💾 Guardar reglas de iptables"
  ansible.builtin.shell: |
    iptables-save > /etc/iptables/rules.v4
  become: true
  changed_when: true

- name: "🔍 Verificar estado de Tayga (sin fallar)"
  ansible.builtin.shell: systemctl is-active tayga || echo "starting"
  register: tayga_status
  become: true
  changed_when: false
  failed_when: false

- name: "✅ NAT64 configurado"
  ansible.builtin.debug:
    msg:
      - "════════════════════════════════════════"
      - "✅ NAT64 (Tayga) configurado"
      - "════════════════════════════════════════"
      - "→ Prefijo NAT64: 64:ff9b::/96"
      - "→ Pool IPv4: 192.168.255.0/24"
      - "→ Interfaz: nat64"
      - "→ Estado: {{ tayga_status.stdout }}"
      - "════════════════════════════════════════"
      - "Verifica con: sudo systemctl status tayga"
      - "Prueba con: ping6 64:ff9b::8.8.8.8"
      - "════════════════════════════════════════"
